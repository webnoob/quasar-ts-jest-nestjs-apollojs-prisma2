# Migration `watch-20191119112151`

This migration has been generated by Allan Gaunt at 11/19/2019, 11:21:51 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Book" (
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."User" (
  "emailAddress" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  "role" INTEGER NOT NULL DEFAULT 0  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "lift"."_canActAs" (
  "A" TEXT    REFERENCES "User"(id) ON DELETE CASCADE,
  "B" TEXT    REFERENCES "User"(id) ON DELETE CASCADE
);

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")

CREATE UNIQUE INDEX "lift"."User.id" ON "User"("id")

CREATE UNIQUE INDEX "lift"."User.username" ON "User"("username")

CREATE UNIQUE INDEX "lift"."User.emailAddress" ON "User"("emailAddress")

CREATE UNIQUE INDEX "lift"."_canActAs_AB_unique" ON "_canActAs"("A","B")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191119112151
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,24 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Book {
+  id    String  @default(cuid()) @id @unique
+  title String
+  description  String?
+}
+
+model User {
+  id String @default(cuid()) @id @unique
+  username String @unique
+  emailAddress String @unique
+  password String
+  role Int
+  canActAs User[] @relation(name: "canActAs")
+  hasActingAs User[] @relation(name: "canActAs")
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191119112151)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191119112151'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
