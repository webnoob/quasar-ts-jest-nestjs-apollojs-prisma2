# Migration `20191015230231-user-table`

This migration has been generated by webnoob at 10/15/2019, 11:02:31 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_User" (
  "emailAddress" TEXT NOT NULL DEFAULT ''  ,
  "id" TEXT NOT NULL   ,
  "password" TEXT NOT NULL DEFAULT ''  ,
  "username" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_User" ("emailAddress","id","password","username") SELECT "emailAddress","id","password","username" from "User"

DROP TABLE "lift"."User";

ALTER TABLE "lift"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "lift"."User.id" ON "User"("id")

CREATE UNIQUE INDEX "lift"."User.username" ON "User"("username")

CREATE UNIQUE INDEX "lift"."User.emailAddress" ON "User"("emailAddress")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Book" (
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Book" ("description","id","title") SELECT "description","id","title" from "Book"

DROP TABLE "lift"."Book";

ALTER TABLE "lift"."new_Book" RENAME TO "Book";

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191014082248-books-main..20191015230231-user-table
--- datamodel.dml
+++ datamodel.dml
@@ -11,4 +11,11 @@
   id    String  @default(cuid()) @id @unique
   title String
   description  String?
 }
+
+model User {
+  id String @default(cuid()) @id @unique
+  username String @unique
+  emailAddress String @unique
+  password String
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191015230231-user-table)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191015230231-user-table'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
