# Migration `20191014082248-books-main`

This migration has been generated by webnoob at 10/14/2019, 8:22:48 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Book" (
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Book" ("description","id","title") SELECT "description","id","title" from "Book"

DROP TABLE "lift"."Book";

ALTER TABLE "lift"."new_Book" RENAME TO "Book";

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration 20191014082136-book..20191014082248-books-main
--- datamodel.dml
+++ datamodel.dml
@@ -6,20 +6,9 @@
 generator photon {
   provider = "photonjs"
 }
-model User {
+model Book {
   id    String  @default(cuid()) @id @unique
-  email String  @unique
-  name  String?
-  posts Post[]
+  title String
+  description  String?
 }
-
-model Post {
-  id        String   @default(cuid()) @id @unique
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-  published Boolean
-  title     String
-  content   String?
-  author    User?
-}
```

## Photon Usage

You can use a specific Photon built for this migration (20191014082248-books-main)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191014082248-books-main'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
