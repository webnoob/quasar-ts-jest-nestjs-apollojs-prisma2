# Migration `20191014073217-book-model`

This migration has been generated by webnoob at 10/14/2019, 7:32:17 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Book" (
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Book" ("description","id","title") SELECT "description","id","title" from "Book"

DROP TABLE "lift"."Book";

ALTER TABLE "lift"."new_Book" RENAME TO "Book";

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "lift"."new_Book" (
  "description" TEXT    ,
  "id" TEXT NOT NULL   ,
  "title" TEXT NOT NULL DEFAULT ''  ,
  PRIMARY KEY ("id")
);

INSERT INTO "new_Book" ("description","id","title") SELECT "description","id","title" from "Book"

DROP TABLE "lift"."Book";

ALTER TABLE "lift"."new_Book" RENAME TO "Book";

CREATE UNIQUE INDEX "lift"."Book.id" ON "Book"("id")

PRAGMA "lift".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..20191014073217-book-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,14 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Book {
+  id    String  @default(cuid()) @id @unique
+  title String
+  description  String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (20191014073217-book-model)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/20191014073217-book-model'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
